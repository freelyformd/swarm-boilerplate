version: '3'

services:
  traefik:
    image: traefik:alpine
    command: --docker --docker.swarmmode --docker.domain=freelyformd.com --docker.watch --web
    ports:
      - 80:80
      - 8080:8080
    networks:
      - traefik-net
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      labels:
        traefik.enable: "true"
        traefik.frontend.rule: 'Host: proxy.freelyformd.com'
        traefik.port: 8080
      placement:
        constraints:
          - node.role == manager

networks:
  traefik-net:
    external: true
